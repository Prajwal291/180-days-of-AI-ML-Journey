# Stage 2 — Structures + OOP (Week 2)

Part of my [180 Days of AI/ML Journey](../README.md).

## What This Stage Covered

- Lists + slicing
- Dictionaries
- Sets + tuples
- Comprehensions
- Classes + objects
- Methods + `__init__`
- `try` / `except`
- Reading + writing files
- Modules + imports
- pip + virtual environments

## What I Built

| Project | Description |
|---|---|
| `BankAccountOOPs.py` | Practice with classes and objects — modeling a bank account with `__init__` and instance methods |
| `ContactBookCLIApp.py` | Stage 2 capstone — a command-line contact book that saves and loads contacts from a file, using classes, dictionaries, and error handling |

## Status

✅ Stage 2 complete — done-when checkpoint met (contact-book CLI app that saves and loads from a file, working end to end).

**Next:** Stage 3 — NumPy (arrays, broadcasting, vectorized operations, matrix multiply).
