# Stage 3 — NumPy 

Part of my [180 Days of AI/ML Journey](../README.md).

## What This Stage Covered

- Arrays + dtypes
- Indexing + slicing
- Broadcasting
- Vectorized operations
- Reshape + shapes
- Matrix multiply (`@`)
- `np.random`
- Aggregations: mean, sum, axis

## What I Built

| File | Description |
|---|---|
| `numpy_1.py` – `numpy_8.py` | Progressive practice exercises covering arrays, indexing/slicing, broadcasting, vectorized operations, reshaping, and aggregations |
| `DataSetNormalization.py` | Normalizing a dataset using vectorized NumPy operations (no explicit loops) |
| `MatrixMultiplication.py` | Matrix multiplication using the `@` operator |

## Key Takeaway

Thinking in whole arrays instead of single values — replacing loops with vectorized operations, and using broadcasting to operate across shapes without manual iteration.

## Status

✅ Stage 3 complete — done-when checkpoint met (normalized a dataset and multiplied matrices without writing a single loop).

**Next:** Stage 4 — Pandas (DataFrames, `read_csv`, filtering, `groupby`, cleaning real data).